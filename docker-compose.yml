name: blog-web

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: blog-web
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    restart: unless-stopped
